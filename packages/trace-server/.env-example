PORT=3000
NODE_ENV=development

DATABASE_URL="mysql://user:password@host:3306/tracking_system"

CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=tracega

# JWT（正式环境务必更换为随机密钥）
JWT_SECRET=dev_tracega_jwt_2024
JWT_EXPIRES_IN=7d

# AI (GLM-4-Flash)
GLM_API_KEY=your_glm_api_key
GLM_MODEL=glm-4-flash
